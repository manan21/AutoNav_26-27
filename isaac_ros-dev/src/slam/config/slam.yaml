slam_toolbox:
  ros__parameters:

    # Plugin params
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # ROS Parameters
    odom_frame: odom
    map_frame: map
    base_frame: base_link
    # PCA-filtered 2-D laser scan (autonav_detection::grade_detector →
    # pointcloud_to_laserscan in slam.launch.py). Using the filtered
    # scan instead of raw /scan_fullframe means SLAM maps only what
    # PCA classifies as obstacles (no ramp faces / drivable grades as
    # walls) — those obstacles end up in /map → /map_padded → global
    # costmap static_layer as persistent long-range obstacles, so the
    # obstacle_layer can stay short-range (2.5 m) for dynamics only.
    # SLAM consumes the raw SICK layer-5 scan, not the PCA-filtered
    # one. Reasons:
    #   1. /scan_pca_filtered is the grade detector's *obstacle* cloud
    #      collapsed to 2D — most returns (drivable ground) are
    #      removed. Fewer points = a looser scan-match constraint and
    #      noisier pose estimates from Ceres.
    #   2. The ground ring from layer-5 is a stable landmark
    #      frame-to-frame; throwing it away hurts SLAM, not helps it.
    #   3. Architectural separation: SLAM now depends only on the
    #      lidar driver. The PCA / grade detector becomes optional —
    #      toggling it off no longer collapses localization (which is
    #      what was happening when /scan_pca_filtered went silent and
    #      slam_toolbox starved → no /pose → no map→odom TF → Nav2
    #      lifecycle stalled at "Activating planner_server").
    # The Nav2 obstacle layer still consumes /scan_pca_filtered via
    # pca_pc2_to_scan — that path is unchanged.
    scan_topic: /scan_fullframe
    use_map_saver: true
    mode: mapping

    debug_logging: false

    # Throttle: only process 1 out of every N scans.
    # The SICK at full rate overwhelms slam_toolbox on the Jetson.
    # Set to 2 or 3 if the lidar publishes at 20 Hz (keeps effective
    # SLAM input at 7-10 Hz which is well within budget).
    throttle_scans: 2

    # How often to broadcast the map->odom TF (seconds).
    # 0.02 = 50 Hz. Fine for real robot; set 0.0 to never publish
    # (use if something else broadcasts this transform).
    transform_publish_period: 0.02

    map_update_interval: 5.0
    # 0.10 m matches what map_padder and global_costmap consume, so /map
    # is published at the target downstream resolution and map_padder's
    # downsample step (factor = round(out_res / slam_res)) becomes 1:1.
    # Local costmap stays at 0.05 m — it's fed by laser scans directly,
    # not by /map, so DWB's narrow-gap precision is unaffected.
    resolution: 0.10
    min_laser_range: 0.3
    max_laser_range: 10.0
    minimum_time_interval: 0.3   # seconds between accepted scans (rate limiter)
    transform_timeout: 0.2
    tf_buffer_duration: 30.0
    stack_size_to_use: 40000000
    enable_interactive_mode: false  # off: saves CPU on Jetson

    # General Parameters
    use_scan_matching: true
    use_scan_barycenter: true
    # Bench-testable defaults: 0.0 means slam_toolbox inserts a keyframe
    # on the first scan it accepts, so the `map` frame appears in TF as
    # soon as scans flow — without requiring the robot to move 30 cm or
    # rotate 0.3 rad first. Insertion rate is still bounded downstream
    # by minimum_time_interval (0.3 s) and throttle_scans (2), so this
    # doesn't spam keyframes.
    minimum_travel_distance: 0.0
    minimum_travel_heading: 0.0
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5
    loop_search_maximum_distance: 3.0
    do_loop_closing: true
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # Correlation Parameters
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # Loop Closure Parameters
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Scan Matcher Parameters
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true